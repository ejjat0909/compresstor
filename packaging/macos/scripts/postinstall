#!/bin/bash
# Post-install script for Compresstor.pkg
# Runs automatically after the app is copied to /Applications.
# Strips quarantine so Gatekeeper doesn't block the app.

xattr -cr /Applications/Compresstor.app 2>/dev/null || true
exit 0
